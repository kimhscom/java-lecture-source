25번 생성자(Constructor)
1) 클래스명과 같고 반환타입이 없는 메소드
2) 클래스를 작성하면 자동으로 보이지는 않지만 생성되어 있음
	(defalut 생성자)
3) heap 위치하고 있음
4) new(인스턴스화)를 할때 딱 1회 호출함
-----------------------------------------------------------------------
5) 오버로딩 : 메소드명은 같고 사용되는 arguments의 갯수 혹은 타입이 다른 경우
	ex) System.out.print(), String.valueOf(),
	만약에 생성자가 오버로딩 되었다면 defalut 생성자는 사라짐
	defalut 사용하고 싶다면 반드시 명시적으로 작성해 줘야 한다.

6) 맴버필드를 초기화 할때 사용함